# 開発ガイドライン & コード規約

このプロジェクトのコード品質を保ち、バグを防ぐための共通ルールです。

## 1. ディレクトリ構成（設計方針）

フロントエンドとバックエンドのコードは、1つのリポジトリ内で分かりやすく分離して管理します。

```text
├── frontend/         # Next.js アプリケーション
└── backend/          # Express アプリケーション
```

## 2. コーディング規約 (TypeScript / JavaScript)

### 2.1 コンポーネントと関数

- コンポーネントや関数は、原則として アロー関数 で記述してください。
- タイプの明示的な指定（型定義）を徹底し、可能な限り any 型の使用は禁止します。

### 2.2 Next.js (App Router) のルール

- サーバーコンポーネント（デフォルト）とクライアントコンポーネント（"use client"）を明確に意識して切り分けてください。
- データの取得（Fetch）は、可能な限りサーバーコンポーネント側で行ってください。

### 2.3 スタイリング

- スタイリングには Tailwind CSS を使用します。
- 原則として独自のCSSファイルは作らず、Tailwindのユーティリティクラスで完結させてください。

## 3. Git / GitHub の運用ルール

### 3.1 コミットメッセージのプレフィックス

- 何をしたコミットなのか一目で分かるよう、メッセージの先頭に以下のプレフィックスを付けてください（メッセージは日本語でOKです）。
- feat: 新しい機能の追加
- fix: バグの修正
- docs: ドキュメント（READMEやTASKSなど）の修正
- style: コードの意味に影響を与えない変更（見た目の調整、フォーマットなど）
- refactor: リファクタリング（機能追加やバグ修正を含まないコードの整理）

## 4. AIアシスタントへの指示

- コードを提案・作成した際は、都度そのコードの解説を日本語で丁寧に行ってください。
